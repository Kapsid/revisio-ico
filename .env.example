APP_NAME="Registry Service"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mariadb
DB_HOST=mariadb
DB_PORT=3306
DB_DATABASE=registry
DB_USERNAME=registry
DB_PASSWORD=secret

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

CACHE_STORE=file

# GUS API (Poland) - well know public key for testing, so its safe to keep it here too
GUS_API_KEY=abcde12345abcde12345
GUS_API_ENV=dev

# ARES (Czech) - no API key required
